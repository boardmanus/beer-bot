theme: jekyll-theme-minimal
title: Beer-Bot
description: A simple site that reads Tilt Hydrometer beacons, and displays the state of fermentation.
defaults:
  - scope:
      path: "docs/imgs"
    values:
      image: true
markdown: "GFM"

